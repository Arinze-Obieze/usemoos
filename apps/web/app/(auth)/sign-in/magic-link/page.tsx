import MagicLinkCallbackHandler from "@/components/auth/MagicLinkCallbackHandler";

export const metadata = {
  title: "Verify sign in · usemoos",
};

export default function MagicLinkPage() {
  return <MagicLinkCallbackHandler />;
}
